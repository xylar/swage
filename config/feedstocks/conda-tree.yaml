feedstock: conda-tree

add_requirements:
  run:
    - line: conda >=25.0
      reason: >-
        conda-tree is a conda plugin, so it needs conda at runtime. Upstream
        declares it in its pixi table, which is not dependency metadata.
