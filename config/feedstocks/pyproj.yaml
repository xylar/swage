feedstock: pyproj

add_requirements:
  host:
    - line: proj
      reason: >-
        pyproj builds against the PROJ C library. The wheels vendor it and the
        conda package links conda-forge's, which python metadata cannot say.
  run:
    - line: proj
      reason: pyproj loads PROJ at runtime and needs the same library it built against.
