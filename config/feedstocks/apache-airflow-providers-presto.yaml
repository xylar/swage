# The same second copy of psycopg2 as the postgres provider carries: upstream's
# `psycopg2-binary` resolves to conda-forge's `psycopg2`, which is already the
# compiled build, leaving the recipe's line under upstream's spelling dead.

feedstock: apache-airflow-providers-presto

retire:
  - psycopg2-binary
