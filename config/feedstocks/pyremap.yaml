feedstock: pyremap

add_requirements:
  run:
    - line: nco >=4.8.1
      reason: pyremap drives nco to apply mapping files, and nco is not on PyPI.
    - line: esmf
      reason: pyremap builds its mapping files with ESMF, which is not on PyPI.
