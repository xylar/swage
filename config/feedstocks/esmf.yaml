# The ESMF archive carries esmpy's pyproject.toml, and esmpy is its own
# feedstock.

feedstock: esmf

upstream:
  source: none
  reason: >-
    esmf builds the ESMF Fortran and C++ library, whose dependencies are stated
    in its makefiles rather than in python metadata.
