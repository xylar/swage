feedstock: mypy-boto3-appflow

add_requirements:
  run:
    - line: boto3
      reason: >-
        type stubs are only usable beside the package they annotate, and
        upstream names boto3 in its keywords rather than its dependencies.
