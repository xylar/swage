feedstock: apache-airflow-providers-google
family: airflow-providers

# 22.4.0 asks for `google-cloud-aiplatform[evaluation]` again, after 22.3.0
# had dropped the extra and listed its dependencies directly. conda-forge's
# google-cloud-aiplatform lists `evaluation` under `skip`, so there is no
# package to depend on and the extra's contents belong here.
#
# Taken from google-cloud-aiplatform 1.165.1, which is what conda-forge
# publishes; 1.164.0, the floor upstream states, declares the same six.
# `pandas` is the seventh and is left out: the provider declares it directly
# at `>=2.1.2` and up, far tighter than the `>=1.0.0` the extra asks for.
#
# Two of the six carry markers that a single noarch package has to answer in
# one line:
#
#   - `scikit-learn` is capped `<1.6.0` on python 3.10 and unconstrained
#     above it. This recipe's python_min is 3.10, so the cap holds for the
#     whole range and the package carries it on every python.
#   - `litellm` is `>=1.83.7,<1.86.0` below python 3.14 and `>=1.93.0,<1.97.0`
#     from 3.14 up -- disjoint, so no single line satisfies both. The later
#     range is the one written here: litellm 1.93.0 supports python 3.10
#     through 3.14, and raising the aiplatform floor to reach it is what
#     upstream's own comment says the 22.4.0 bump was for, citing
#     CVE-2026-35030 and the advisories following it.
embedded_extras:
  "google-cloud-aiplatform[evaluation]":
    - tqdm >=4.23.0
    - scikit-learn <1.6.0
    - jsonschema
    - ruamel.yaml
    - pyyaml
    - litellm >=1.93.0,<1.97.0

# Upstream declared `PyOpenSSL>=23.0.0` through 19.1.0, the version this
# recipe still reflects, and dropped it in 21.0.0. Nothing in 22.4.0 accounts
# for the line.
retire:
  - pyopenssl
